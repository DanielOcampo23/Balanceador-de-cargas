worker_processes  1;
events {
   worker_connections 1024;
}

http {
    upstream servers {
         server <%=@ip%>;
         server <%=@ip2%>;
    }

    server {
        listen 8080;

        location / {
              proxy_pass http://servers;
        }
    }
}

